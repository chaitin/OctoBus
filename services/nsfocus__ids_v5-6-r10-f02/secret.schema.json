{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "cookie": { "type": "string", "description": "NSFOCUS IDS web session cookie header value. Time-limited; refresh when expired." },
    "sessionCookie": { "type": "string", "description": "Alias for cookie." },
    "session_cookie": { "type": "string", "description": "Alias for cookie." }
  }
}
