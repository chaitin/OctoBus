{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "required": ["baseUrl"],
  "properties": {
    "baseUrl": {
      "type": "string",
      "format": "uri",
      "description": "Base URL of the authorized Insight deployment."
    },
    "rpcPath": {
      "type": "string",
      "default": "/pedestal/rpc",
      "description": "Insight JSON-RPC endpoint path."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1000,
      "maximum": 120000,
      "default": 10000
    },
    "skipTlsVerify": {
      "type": "boolean",
      "default": false,
      "description": "Disable TLS certificate verification for controlled private deployments only."
    },
    "sendJwtCookie": {
      "type": "boolean",
      "default": true,
      "description": "Send the API token as the jwt cookie in addition to Bearer authentication, matching chaitin-cli."
    }
  }
}
