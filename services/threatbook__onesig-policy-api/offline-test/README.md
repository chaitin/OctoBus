# Offline Test

Use this directory for customer-site read-only checks. Start with status or list methods before running any write operation.

Recommended first checks:

- `DevicePlatformStatus`
- `DeviceSystemStatus`
- `ListGlobalBlacklist`
- `ListGlobalWhitelist`
