{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "user": {
      "type": "string",
      "description": "SecVSS 3600 扫描器登录用户名（可选，也可在 GetToken 请求参数中传入）"
    },
    "pwd": {
      "type": "string",
      "description": "SecVSS 3600 扫描器登录密码（可选，也可在 GetToken 请求参数中传入）"
    }
  }
}
