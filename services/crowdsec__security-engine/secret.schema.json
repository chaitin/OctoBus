{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "machineId": {
      "type": "string",
      "description": "Crowdsec machine ID for JWT authentication (used for alerts and decision management)."
    },
    "password": {
      "type": "string",
      "description": "Crowdsec machine password for JWT authentication."
    },
    "apiKey": {
      "type": "string",
      "description": "Crowdsec bouncer API key (X-Api-Key header, used for reading decisions)."
    }
  }
}
