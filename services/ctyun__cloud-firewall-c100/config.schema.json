{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "regionId"
  ],
  "properties": {
    "endpoint": {
      "type": "string",
      "default": "https://ctcfw-east-a.ctapi.ctyun.cn",
      "description": "Optional CTYun Cloud Firewall endpoint override. The documented global endpoint is https://ctcfw-global.ctapi.ctyun.cn."
    },
    "regionId": {
      "type": "string",
      "description": "CTYun resource pool ID sent as the regionid request header."
    },
    "urlType": {
      "type": "string",
      "default": "CTAPI",
      "description": "CTYun request URL type header."
    },
    "timeoutMs": {
      "type": "integer",
      "minimum": 1,
      "default": 5000,
      "description": "HTTP timeout in milliseconds."
    },
    "headers": {
      "type": "object",
      "additionalProperties": {
        "type": "string"
      },
      "description": "Optional additional HTTP headers. EOP signing headers are set by the service."
    }
  }
}
